-- ============================================================
-- Migration: classrooms.number_mode
-- Date:     2026-06-20
-- Reason:   เรียงเลขที่นักเรียนแบบแยกเพศ (ชาย-หญิง / หญิง-ชาย)
--           เพิ่มคอลัมน์เก็บโหมดการเรียงต่อห้อง · ปุ่ม "เรียงเลขที่ใหม่"
--           ในหน้าจัดการนักเรียนใช้ค่านี้ · ทุก flow (เพิ่ม/ย้าย/นำเข้า/ลบ)
--           เคารพโหมดนี้อัตโนมัติ
--
-- Run via Supabase Dashboard → SQL Editor (single transaction).
-- โรงเรียนที่ deploy ก่อน 2026-06-20 ต้องรันไฟล์นี้หลัง Sync fork.
-- ============================================================

BEGIN;

ALTER TABLE classrooms
  ADD COLUMN IF NOT EXISTS number_mode TEXT DEFAULT 'code';

COMMENT ON COLUMN classrooms.number_mode IS 'การเรียงเลขที่นักเรียน: code=ตามรหัส, male_first=ชาย-หญิง, female_first=หญิง-ชาย';

COMMIT;

-- ============================================================
-- After running:
--   1. Table Editor → classrooms → ต้องเห็น column ใหม่ (number_mode)
--   2. หน้าจัดการนักเรียน → ปุ่ม "เรียงเลขที่ใหม่" → เลือกโหมด ชาย-หญิง
--   3. เพิ่ม/ย้าย/นำเข้านักเรียน → เลขที่เรียงตามโหมดที่ตั้งไว้อัตโนมัติ
-- ============================================================
